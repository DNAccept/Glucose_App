# Glucose App Online Database Server Environment Configuration

# Server HTTP Port (Default: 8080 or assigned by cloud platform)
PORT=8080

# SQLite Database Absolute File Path
# For Docker / Cloud Hosting, mount a persistent volume at /data
DATABASE_PATH=/data/online_database.sqlite

# CORS Allowed Origins (* for mobile app clients or specific domain comma-separated)
ALLOWED_ORIGINS=*
